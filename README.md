# CRUD Operations with JSON

This project is a simple Node.js and Express CRUD API that stores data in a `data.json` file.

## Endpoints

- `GET /items`
- `GET /items/:id`
- `POST /items`
- `PUT /items/:id`
- `PATCH /items/:id`
- `DELETE /items/:id`

## Run

```bash
npm install
npm start
```
